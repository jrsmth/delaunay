name: build-tag-deploy
on:
  push:
    branches: [ main, feature/demo/DEL-40 ]
permissions:
  contents: write
jobs:
  build-tag-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧
        working-directory: ./app
        run: |
          npm ci
          npm run build
          cp .gitignore ../.gitignore

      - name: Bump and Tag 🏷️
        uses: phips28/gh-action-bump-version@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PACKAGEJSON_DIR: './app'

      - name: Add -SNAPSHOT (X) # TODO
        working-directory: ./app
        run: |
          npm version
          npm version $(cat package.json | jq -r '.version')-SNAPSHOT

      - name: Commit -SNAPSHOT (X) # TODO
        uses: stefanzweifel/git-auto-commit-action@v4

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .

# Courtesy of:
# https://github.com/marketplace/actions/deploy-to-github-pages
# https://github.com/marketplace/actions/automated-version-bump
# ... TODO


# these are the config words...
#config words: {
#  majorWords: [ 'BREAKING CHANGE', 'major' ],
#  minorWords: [ 'feat', 'minor' ],
#  patchWords: null,
#  preReleaseWords: [ 'pre-alpha', 'pre-beta', 'pre-rc' ]
#}
# Perhaps these are the default and you can overwrite them with an obscure option...?


# TODO: rm css and js again! .gitignore copy in the wrong place (need twice?)


# Question :: Why about writing your own MAKE script that can be called locally (for MAJOR/MINOR) and from GH (for PATCH)
# Steps :: drop snapshot, tag, bump version, append snapshot, commit
# Could turn this into your own github action (npm-package-bump)? Muntjac it... Probs worth a couple of weeks of Spring...
# Example: https://github.com/relativityboy/npm-auto-snapshot